/* General Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Roboto', sans-serif;
  line-height: 1.6;
  color: #333;
}

/* Header Section */
.header {
  background: linear-gradient(90deg, #00457C, #0066CC);
  color: #fff;
  padding: 2rem 0;
}

.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 2rem;
}

.navbar .logo {
  font-size: 1.8rem;
  font-weight: bold;
  text-decoration: none;
  color: #fff;
}

.navbar .nav-links {
  list-style: none;
  display: flex;
  gap: 1.5rem;
}

.navbar .nav-links a {
  color: #fff;
  text-decoration: none;
  font-weight: 500;
}

.navbar .call-button {
  padding: 0.5rem 1rem;
  background: #FFD700;
  color: #333;
  text-decoration: none;
  border-radius: 5px;
  font-weight: bold;
}

.hero-content {
  text-align: center;
}

.hero-content h1 {
  font-size: 2.5rem;
  margin-bottom: 1rem;
}

.hero-content p {
  font-size: 1.2rem;
  margin-bottom: 2rem;
}

.cta-button {
  padding: 0.8rem 1.5rem;
  background: #FFD700;
  color: #333;
  text-decoration: none;
  font-weight: bold;
  border-radius: 5px;
}

.cta-button:hover {
  background: #FFC107;
}

/* Section Styling */
.container {
  width: 90%;
  max-width: 1200px;
  margin: 0 auto;
  padding: 2rem 0;
}

h2 {
  text-align: center;
  margin-bottom: 1.5rem;
  color: #00457C;
}

p {
  margin-bottom: 1rem;
}

/* Services Section */
.services {
  background: #f9f9f9;
}

.service-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
}

.service-card {
  background: #fff;
  padding: 1.5rem;
  border-radius: 8px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
  text-align: center;
}

.service-card h3 {
  color: #00457C;
  margin-bottom: 0.5rem;
}

.service-card p {
  font-size: 0.9rem;
}

/* Contact Section */
.contact {
  text-align: center;
}

.contact a {
  color: #00457C;
  text-decoration: none;
  font-weight: bold;
}

.contact a:hover {
  text-decoration: underline;
}

/* Footer */
.footer {
  background: #333;
  color: #fff;
  text-align: center;
  padding: 1rem 0;
  font-size: 0.9rem;
}
